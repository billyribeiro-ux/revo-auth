# Revo-Auth server URL (run `docker run ...` to start locally).
REVO_AUTH_SERVER_URL=http://localhost:8787

# Application credentials issued by the auth server / CLI.
REVO_AUTH_APP_ID=demo-app
REVO_AUTH_PUBLIC_KEY=pk_demo_replace_me
REVO_AUTH_SECRET_KEY=sk_demo_replace_me
