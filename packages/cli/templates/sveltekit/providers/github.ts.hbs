import { defineProvider } from '@revo-auth/sdk-sveltekit/providers';

export default defineProvider({
	id: 'github',
	enabled: true,
	clientId: process.env.GITHUB_CLIENT_ID ?? '',
	clientSecret: process.env.GITHUB_CLIENT_SECRET ?? '',
	scope: ['read:user', 'user:email'],
});
