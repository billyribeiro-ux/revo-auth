import type { Session, User } from '@revo-auth/sdk-sveltekit';

declare global {
	namespace App {
		interface Locals {
			session: Session | null;
			user: User | null;
		}
	}
}

export type { Session, User };
