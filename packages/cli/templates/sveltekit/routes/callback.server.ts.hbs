import { handleOAuthCallback } from '@revo-auth/sdk-sveltekit/server';
import type { RequestHandler } from '@sveltejs/kit';

export const GET: RequestHandler = async (event) => {
	return await handleOAuthCallback(event, { redirectTo: '/' });
};
