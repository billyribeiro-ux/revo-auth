import { defineAuthConfig } from '@revo-auth/sdk-sveltekit';

/**
 * Generated by `revo-auth init` (preset: {{preset}}).
 * This file is user-owned — `revo-auth update` will only add new keys with
 * defaults, never overwrite existing values.
 */
export default defineAuthConfig({
	serverUrl: process.env.REVO_AUTH_SERVER_URL ?? '{{serverUrl}}',
	appId: process.env.REVO_AUTH_APP_ID ?? '{{appId}}',
	session: {
		strategy: '{{session}}',
	},
	methods: {
{{#if (includes methods "email")}}		email: { enabled: true },
{{/if}}{{#if (includes methods "google")}}		google: { enabled: true },
{{/if}}{{#if (includes methods "github")}}		github: { enabled: true },
{{/if}}{{#if (includes methods "microsoft")}}		microsoft: { enabled: true },
{{/if}}{{#if (includes methods "apple")}}		apple: { enabled: false },
{{/if}}{{#if (includes methods "discord")}}		discord: { enabled: true },
{{/if}}{{#if (includes methods "passkeys")}}		passkeys: { enabled: true },
{{/if}}{{#if (includes methods "totp")}}		totp: { enabled: true },
{{/if}}{{#if (includes methods "magic-link")}}		magicLink: { enabled: true },
{{/if}}	},
	features: {
{{#if (includes features "organizations")}}		organizations: { enabled: true },
{{/if}}{{#if (includes features "audit-log")}}		auditLog: { enabled: true },
{{/if}}{{#if (includes features "custom-fields")}}		customFields: { enabled: true },
{{/if}}{{#if (includes features "anonymous-mode")}}		anonymousMode: { enabled: true },
{{/if}}{{#if (includes features "no-analytics")}}		analytics: { enabled: false },
{{/if}}	},
});
